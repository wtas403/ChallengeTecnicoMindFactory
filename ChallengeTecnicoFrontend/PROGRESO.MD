# Progreso

## Estado actual
- Etapa 1 implementada: shell minimo con `RouterOutlet`.
- Etapa 2 implementada: estructura base de la feature `automotores`.
- Etapa 3 implementada: tipos de dominio auxiliares + validadores puros + tests unitarios.
- Etapa 4 implementada: infraestructura y contrato de integracion con backend Express sobre `/api`.
- Etapa 5 implementada: estado y orquestacion de listado con signals, facade y estados de UI.
- Etapa 6 implementada: pantalla de listado consolidada con rutas de crear/editar y accion de eliminar.
- Etapa 7 implementada: formulario crear/editar con `AutomotorDraft`, validaciones y guard de cambios.
- Etapa 8 implementada: flujo de titular por CUIT con alta inline de sujeto y reintento automatico.
- Etapa 9 implementada: mejoras de UX/accesibilidad con `id` estables para futura automatizacion en Playwright.
- Estructura objetivo completada:
  - `core/`
  - `shared/`
  - capas internas de `automotores` (`domain`, `application`, `infrastructure`).

## Cambios realizados

### 2026-04-09
- Se reemplazo el scaffold inicial de Angular por un shell minimo.
- Se configuro lazy loading hacia `features/automotores`.
- Se creo la pagina inicial `automotores-list-page`.
- Se creo el componente base `automotores-list`.
- Se agregaron modelos iniciales:
  - `automotor`
  - `titular`
- Se creo un store inicial de automotores.
- Se creo el contrato inicial de repositorio de automotores.
- Se completo la estructura objetivo de carpetas:
  - `core`
  - `shared`
  - `domain/validators`
  - `domain/value-objects`
  - `domain/errors`
  - `application/stores`
  - `application/facades`
  - `application/use-cases`
  - `infrastructure/api`
  - `infrastructure/dtos`
  - `infrastructure/mappers`
  - `infrastructure/repositories`
- Se reubicaron archivos para alinear la estructura:
  - `automotores-store`
  - `automotores-repository`
- Se agregaron tipos auxiliares en `domain/types`:
  - `automotores-filters`
  - `automotores-sort`
  - `automotores-page`
- Se implementaron validadores de dominio en `domain/validators`:
  - `dominio.validator`
  - `cuit.validator`
  - `fecha-fabricacion.validator`
- Se agregaron tests unitarios de validadores:
  - dominio
  - CUIT
  - fecha fabricacion YYYYMM
- Se agrego configuracion base de API en `core/config/api.config` con base URL `/api`.
- Se agrego manejo transversal de errores HTTP en `core/http`:
  - `api-error`
  - `api-error.mapper`
  - `api-error.interceptor`
- Se configuraron providers globales en `app.config`:
  - `provideHttpClient` con interceptor de errores
  - `API_BASE_URL` en `/api`
- Se implementaron DTOs de infraestructura para automotores y sujetos.
- Se implementaron mappers DTO <-> dominio para automotor y titular.
- Se implementaron servicios API:
  - `automotores-api.service`
  - `sujetos-api.service`
- Se implementaron repositorios HTTP:
  - `http-automotores-repository`
  - `http-titulares-repository`
- Se definio estrategia de no-existencia de sujeto por CUIT:
  - API responde `404`
  - repositorio de titulares traduce a `null`
- Se agregaron tests unitarios de infraestructura:
  - `api-error.mapper`
  - `automotor.mapper`
  - `titular.mapper`
- Se agrego token de inyeccion `AUTOMOTORES_REPOSITORY` para desacoplar `application` de implementacion HTTP.
- Se implemento `AutomotoresListFacade` para orquestar carga, recarga, filtros, orden y paginacion.
- Se evoluciono `AutomotoresStore` para modelar estado de listado:
  - `idle`
  - `loading`
  - `success`
  - `error`
- Se agregaron use-cases puros de consulta en memoria:
  - `filterAutomotores`
  - `sortAutomotores`
  - `paginateAutomotores`
  - `calculateTotalPages`
- Se conecto `automotores-list` al facade con UI real:
  - barra de busqueda por dominio/CUIT
  - estados `loading/error/empty/success`
  - tabla con ordenamiento por columnas principales
  - paginacion client-side
  - acciones placeholder de editar/eliminar listas para enrutar en etapas siguientes
- Se agregaron tests de Etapa 5:
  - `automotores-list-facade.spec`
  - `query-automotores.spec`
- Se ampliaron rutas de automotores:
  - `GET /` (listado)
  - `/crear`
  - `/:dominio/editar`
- Se creo `automotor-form-page` placeholder para destrabar navegacion de crear/editar en la Etapa 6.
- Se mejoro `automotores-list-page`:
  - encabezado con descripcion
  - CTA principal `Crear automotor`
- Se conectaron acciones de listado:
  - `Editar` navega a `/:dominio/editar`
  - `Eliminar` ejecuta confirmacion y baja real via facade/repositorio
- Se agrego seguimiento de eliminacion por dominio para evitar dobles envios.
- Se agregaron tests de componente para estados visuales del listado:
  - loading
  - empty
  - error
  - render de filas
- Se introdujo `AutomotorDraft` como modelo de entrada para formularios y escrituras.
- Se adaptaron contratos y mappers de infraestructura:
  - repositorio de automotores recibe `AutomotorDraft` en `create/update`
  - `mapAutomotorToDraft`
  - `mapAutomotorDraftToCreateAutomotorDto`
  - `mapAutomotorDraftToUpdateAutomotorDto`
- Se agrego token de inyeccion `TITULARES_REPOSITORY` para orquestacion del formulario.
- Se implemento `AutomotorFormStore` para modelar estado de formulario:
  - carga
  - submit
  - delete
  - lookup de titular por CUIT
- Se implemento `AutomotorFormFacade` con paso previo obligatorio de lookup de titular por CUIT antes de create/update.
- Se implemento `automotor-form` con formulario reactivo tipado y validaciones client-side de dominio/CUIT/fecha.
- Se reemplazo placeholder de `automotor-form-page` por flujo real de crear/editar/eliminar.
- Se agrego guard de cambios pendientes en rutas de crear/editar:
  - `automotor-pending-changes.guard`
- Se agregaron tests de Etapa 7:
  - `automotor-form-facade.spec`
  - `automotor-form.spec`
  - ampliacion de `automotor.mapper.spec`
- Se extendio `AutomotorFormStore` para soportar:
  - estado de creacion de titular inline
  - submit pendiente para reintento
  - estado de reasignacion por cambio de CUIT en edicion
- Se extendio `AutomotorFormFacade` para flujo completo cuando CUIT no existe:
  - lookup previo por CUIT
  - apertura de alta de sujeto
  - `createTitularAndRetry` con reintento automatico de `create/update`
- Se creo componente `crear-titular-inline` con formulario minimo:
  - `cuit`
  - `nombreCompleto`
- Se integro `crear-titular-inline` en `automotor-form-page`:
  - muestra contextual cuando no existe sujeto
  - alta de sujeto y continuidad del flujo sin reenvio manual del automotor
- Se agrego indicador visual de reasignacion cuando cambia CUIT en modo editar.
- Se agregaron tests de Etapa 8:
  - cobertura de retry en `automotor-form-facade.spec`
  - `crear-titular-inline.spec`
- Se reforzaron formularios con accesibilidad por campo:
  - `aria-invalid`
  - `aria-describedby`
  - ids de error por control
- Se agrego manejo de foco al primer campo invalido en submit de `automotor-form`.
- Se incorporaron `id` semanticos y estables en elementos clave del listado y formulario para Playwright.
- Se agregaron ids por fila para acciones criticas en listado:
  - `automotor-<dominio>-edit`
  - `automotor-<dominio>-delete`
- Se incorporo foco inicial automatico en `crear-titular-inline` al habilitar el flujo.
- Se ampliaron tests de componentes para validar atributos ARIA e ids clave:
  - `automotor-form.spec`
  - `automotores-list.spec`
  - `crear-titular-inline.spec`

## Verificaciones
- `npm run build` OK
- `npm run test -- --watch=false` OK

## Proximo paso
- Etapa 10: consolidar manejo de errores y mapeo de `422` con mensajes accionables de punta a punta.
